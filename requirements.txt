# Floor Tiles Showroom — Python dependencies (Flask / WSGI)
# Install: pip install -r requirements.txt
# Compatible with Python 3.11–3.14 (use a project .venv)

flask>=3.0.0
gunicorn>=22.0.0
jinja2>=3.1.5
pydantic>=2.11.0
pydantic-settings>=2.7.0
itsdangerous>=2.2.0
openpyxl>=3.1.0
fpdf2>=2.7.0
# Optional / legacy helpers (safe to keep)
aiosqlite>=0.20.0
