# Brand & design assets

Place source design files here (not served publicly by FastAPI).

Suggested contents:

- Logo master files (SVG, AI, PNG)
- Brand guidelines PDF
- High-resolution product photography (before export to `static/images/`)
- Showroom video masters (export compressed MP4 to `static/videos/`)

Public web assets belong in `static/`.
